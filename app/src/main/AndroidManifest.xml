<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.calvin.lifestyle">
    <uses-permission android:name="com.android.alarm.permission.SET_ALARM"/>﻿

    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS"
                     tools:ignore="ProtectedPermissions"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".IntroPage">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Water" />
        <activity android:name=".MotivationPage"/>
        <activity android:name=".ProductivityPage"/>
        <activity android:name=".SettingsPage"/>
        <activity android:name=".WaterSettings"/>
        <activity android:name=".ProductivitySettings"/>
        <activity android:name=".MotivationSettings"/>

        <receiver android:name=".NotificationReceiver"/>
    </application>


</manifest>